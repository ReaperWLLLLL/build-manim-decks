[project]
name = "{{PROJECT_SLUG}}"
version = "0.1.0"
description = "Manim presentation project generated by build-manim-decks"
requires-python = ">=3.10"
dependencies = [
    "manim>=0.19",
    "manim-slides==5.6.0",
    "PyYAML>=6.0",
    "Pillow>=10.0",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
