# Text humanization review: {{PROJECT_TITLE}}

Text review: pending

Run `scripts/audit_text.py`, apply the general and locale-specific humanization
passes, preserve evidence and terminology, synchronize visible scene text, and
re-render affected slides before changing this status to `approved`.
